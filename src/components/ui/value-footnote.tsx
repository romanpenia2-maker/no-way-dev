/** Explains the "Value †" column wherever it appears. Server-safe. */
export function ValueFootnote() {
  return (
    <>
      <span className="font-bold">Value</span> <sup className="font-bold">†</sup> = arena score per $1 of blended
      price (3:1 input/output mix)
    </>
  );
}
